## Packages
framer-motion | Page transitions and complex animations
clsx | Conditional class names utility
tailwind-merge | Merging tailwind classes safely

## Notes
- Using standard HTML5 file input with `capture="user"` for camera access on mobile.
- API endpoints expect `multipart/form-data` for image uploads.
- The theme is dark mode by default as requested (#0A1628 background).
